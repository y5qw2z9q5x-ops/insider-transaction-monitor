[project]
name = "insider-transaction-monitor"
version = "0.1.0"
description = "Daily SEC insider transaction monitor for a dynamic US stock list."
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
